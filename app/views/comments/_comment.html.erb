
	
	<span><%= comment.user.email %>:  &nbsp &nbsp<%= comment.content %>&nbsp &nbsp &nbsp <%= time_ago_in_words(comment.created_at) %> &nbsp<span>
	
	<% if current_user == comment.user %>
		<%= link_to 'Destroy', comment_path(comment),
            data: {:confirm => 'Are you sure?'}, :method => :delete %>
		<hr>
	<% end %>
	
